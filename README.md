# Love Story

Персональный сайт-альбом про пару на Django + TailwindCSS.

## Стек
- Python 3.11+
- Django 4.2+
- TailwindCSS (CDN)
- Vanilla JavaScript
- Pillow для работы с изображениями

## Быстрый старт
1. Создайте и активируйте виртуальное окружение:
   ```bash
   python -m venv .venv
   .\.venv\Scripts\activate  # Windows
   source .venv/bin/activate # macOS / Linux
   ```
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Примените миграции и создайте суперпользователя:
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   python manage.py createsuperuser
   ```
4. Запустите проект:
   ```bash
   python manage.py runserver
   ```
5. Перейдите на `http://127.0.0.1:8000/admin/`, заполните модели и посмотрите сайт на главной странице.

## Основные возможности
- Нежный pastel-romantic дизайн, анимация появления блоков, плавающие сердечки, плавные переходы между страницами.
- Главная страница с героем, подсчётом «Вместе уже X дней 💕» и быстрыми ссылками к разделам.
- Таймлайн событий, фотогалерея с lightbox, цитаты и интерактивный список планов (AJAX `/api/plan/<id>/toggle/`).
- Кастомная панель `/dashboard/` в романтичном стиле со статистикой, CRUD для всех моделей и Cropper.js при загрузке изображений.
- Админка `/admin/` остаётся доступной; в ней отображается предпросмотр изображений и ограничение на единственную запись `MainInfo`.

## Медиа и файлы
- Загрузки сохраняются в папку `media/`.
- Убедитесь, что директория существует и у сервера есть права записи.

## Внутренняя панель
- Авторизуйтесь на `/dashboard/login/` с учётными данными суперпользователя.
- После входа откроется `/dashboard/home/` со статистикой и навигацией.
- В разделах можно создавать, редактировать и удалять события, фото (с обрезкой), цитаты, планы и главный блок.

## Структура проекта
```
love_story/
├── love_story/        # Настройки Django-проекта
├── memories/          # Приложение с моделями, вьюхами, шаблонами и статикой
├── media/             # Каталог для загружаемых изображений
├── templates/         # Общие шаблоны (пустой, если не используется)
├── manage.py
└── requirements.txt
```

## Дальнейшая кастомизация
- Добавьте свои шрифты или цвета через Tailwind CDN-конфигурацию в `base.html`.
- Дополняйте шаблоны Tailwind-классами или собственным CSS/JS.
- Используйте админку, чтобы быстро управлять событиями, фотографиями, цитатами и планами.


## Развёртывание на Render

### Локальная подготовка
- Скопируйте .env.example в .env и подставьте значения для разработки.
- Установите зависимости pip install -r requirements.txt, выполните python manage.py migrate и убедитесь, что проект стартует локально.

### Публикация в Git
- Создайте репозиторий и запушьте проект вместе с файлами 
ender.yaml и .env.example.
- Убедитесь, что выбранная ветка деплоя (например, main) содержит актуальные миграции.

### Создание Blueprint на Render
1. В консоли Render выберите **New ➜ Blueprint** и укажите репозиторий.
2. Render считает 
ender.yaml и предложит создать web‑сервис love-story и базу love-story-db.
3. На этапе конфигурации:
   - Убедитесь, что выбран план Free (или смените при необходимости).
   - Обновите DJANGO_ALLOWED_HOSTS, указав фактический домен (<имя-сервиса>.onrender.com, можно несколько через запятую).
   - В DJANGO_CSRF_TRUSTED_ORIGINS перечислите https://<имя-сервиса>.onrender.com и дополнительные домены.
4. Проверьте, что DJANGO_SECRET_KEY настроен на **Generate**, а DATABASE_URL привязан к базе love-story-db.
5. Подтвердите создание Blueprint — Render выполнит первый деплой.

### Первичная инициализация
- После запуска откройте вкладку **Shell** и выполните:
`ash
python manage.py migrate
python manage.py createsuperuser
`
- При необходимости повторите python manage.py collectstatic --noinput или запустите команду вручную.

### Автодеплой
- Любой git push в выбранную ветку инициирует новый деплой.
- Команда запуска — python manage.py runserver 0.0.0.0:; для серьёзной нагрузки лучше перейти на Gunicorn.

### Дополнительные замечания
- collectstatic запускается при сборке, статикой управляет WhiteNoise.
- DATABASE_URL переключает проект на PostgreSQL; SQLite на Render не гарантирует постоянства данных.
- Файлы из media/ хранятся на локальном диске контейнера и могут быть удалены при рестарте — подключите внешнее хранилище при необходимости.
